<%= form_for @freelancer_profile do |f| %>

  <%= f.label :business %>
  <%= f.text_field :business %>

  <%= f.label :field %>
  <%= f.text_field :field %>

  <%= f.submit (@freelancer_profile.new_record? ? "Create" : "Update") %>

<% end %>
