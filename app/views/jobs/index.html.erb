<% @jobs.each do |job| %>

  <%= job.title %>
  <% if can? :read job %>
  <%= link_to "See job", job_path(job.id) %>

  <% if can? :update, job %>
    <%= link_to "Edit job", edit_job_path( job.id ) %>
  <% end %>

  <% if can? :destroy, job %>
    <%= link_to "Delete job", job, method: :delete, data: {confirm: "Do you wish to delete #{job.title}?"} %>
  <% end %>

<% end %>

<%= if can? :create, job %>
  <%= link_to "New job", new_job_path %>
<% end %>
